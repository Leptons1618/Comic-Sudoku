import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.comicsudoku.app',
  appName: 'Comic Sudoku Solver',
  webDir: 'dist'
};

export default config;
